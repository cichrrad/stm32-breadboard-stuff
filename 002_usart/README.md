# Notes

- Demo for communication with the board over ST-link virtual COM port via tools such as `minicom` (see `usart` macro in `Makefile`).
- Uses overlay `struct`s to map onto registers nicely and show *HAL*-esque way of working with them.